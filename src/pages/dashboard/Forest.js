import StatCards from "../../components/dashboard/StatCards"

const Forest = ({setPageTitle}) => {
    setPageTitle('Forest')
    return (
        <>
            <StatCards />
        </>
    )
}

export default Forest
